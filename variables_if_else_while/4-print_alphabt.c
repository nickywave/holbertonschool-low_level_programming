#include <stdio.h>

/**
 * main - entry point of the program where the execution begins
 *
 * Return: Always 0 (Success)
 *
 */

int main () 
{
   char ch;

   if (ch !='q' && ch !='e') 
   for(ch = 'a' ; ch <= 'z' ; ch++)
 
   putchar(ch);
 

   return(0);
}
