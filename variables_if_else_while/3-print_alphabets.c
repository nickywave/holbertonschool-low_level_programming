#include <stdio.h>

in - entry point of the program where the execution begins
 *
 *  * Return: Always 0 (Success)
 *   *
 *    */:char ch;

   for(ch = 'a' ; ch <= 'z' ; ch++) {
      putchar(ch);
   }

           putchar(ch);

   return(0);
}
